# 🚀 My Code Assistant - Промпт для реализации

## 📌 ВИДЕНИЕ ПРОЕКТА

Создать **универсальный инструмент** (framework/CLI tool), который может быть внедрён в **ЛЮБОЙ проект** для создания личного кода ассистента. Этот ассистент:

- Индексирует исходный код и документацию проекта через RAG
- Понимает структуру проекта через git
- Помогает разработчику разбираться с кодом, архитектурой, реализацией фич
- Отвечает с цитатами на источники (исходный код, документация)
- Интегрируется в проект через простую конфигурацию

**Главная идея**: Вместо одноразового ассистента для конкретного проекта, создать **переиспользуемый инструмент**.

---

## 🎯 ЧЕТЫРЕ ГЛАВНЫЕ КОМПОНЕНТЫ

### 1. **ProjectIndexer** - Индексирует ЛЮБОЙ проект
```typescript
// Умеет:
// - Читать конфиг projectConfig.json
// - Выбирать какие папки индексировать (src/, docs/, tests/)
// - Исключать папки (node_modules, .git, build/)
// - Индексировать различные типы файлов (.ts, .js, .md, .json)
// - Пересобирать индекс при изменениях

export class ProjectIndexer {
  constructor(configPath: string);
  async indexProject(): Promise<void>;
  async reindex(): Promise<void>;
  getIndexStats(): IndexStats;
}
```

### 2. **GitHelper** - Получает информацию из git репозитория
```typescript
// Стандартный MCP инструмент для работы с git
// Умеет:
// - Получить текущую ветку
// - Получить историю коммитов
// - Получить статус файлов
// - Получить информацию о последних изменениях
// - Работать с git blame

export class GitHelper {
  constructor(repoPath: string);
  async getCurrentBranch(): Promise<string>;
  async getLastCommits(count: number): Promise<CommitInfo[]>;
  async getFileHistory(filepath: string): Promise<CommitInfo[]>;
  async getProjectStats(): Promise<ProjectStats>;
}
```

### 3. **CodeAssistant** - Главный класс, оркестрирует всё
```typescript
// Объединяет:
// - RAG индекс проекта
// - Git информацию
// - Conversation память
// - LLM с цитатами

export class CodeAssistant {
  private indexer: ProjectIndexer;
  private git: GitHelper;
  private rag: RAGPipeline;
  private conversationManager: ConversationManager;

  constructor(configPath: string);
  async initialize(): Promise<void>;
  async ask(question: string): Promise<AnswerWithSources>;
  async getProjectContext(): Promise<ProjectContext>;
}
```

### 4. **ChatBot CLI** - Интерактивный интерфейс
```typescript
// Параметризуемый чатбот который:
// - Использует CodeAssistant для ответов
// - Поддерживает команды /help, /history, /git-status, etc.
// - Показывает контекст проекта при старте
// - Форматирует ответы с источниками

export class ChatBotCLI {
  constructor(assistant: CodeAssistant);
  async start(): Promise<void>;
  // /help - справка по использованию
  // /git - информация о текущей ветке и коммитах
  // /history - история разговора
  // /search - полнотекстовый поиск в коде
}
```

---

## 📋 КОНФИГУРАЦИЯ ПРОЕКТА

### projectConfig.json - единственный файл, который настраивает всё
```json
{
  "projectName": "my-awesome-project",
  "projectDescription": "My Node.js/TypeScript project",

  "paths": {
    "root": ".",
    "git": ".",
    "output": "node_modules/.code-assistant"
  },

  "indexing": {
    "includeFolders": ["src", "docs", "config"],
    "excludeFolders": ["node_modules", ".git", "build", "dist", ".next", "venv"],
    "includeFileTypes": [".ts", ".tsx", ".js", ".md", ".json", ".py"],
    "excludePatterns": ["*.test.*", "*.spec.*", "*.min.js"],
    "maxFileSize": "1MB",
    "chunkSize": 400,
    "chunkOverlap": 100
  },

  "git": {
    "enabled": true,
    "includeCommitHistory": true,
    "maxCommitsToFetch": 50
  },

  "llm": {
    "model": "llama3.2",
    "temperature": 0.2,
    "topP": 0.8,
    "contextWindow": 20,
    "maxResults": 5
  },

  "prompt": {
    "system": "You are a code assistant for {projectName}. Help understand the codebase, explain architecture, suggest implementations. Always cite sources.",
    "language": "en"
  },

  "mcp": {
    "enabled": true,
    "port": 3000,
    "tools": ["git", "file-search", "code-analysis"]
  }
}
```

---

## 🗂️ АРХИТЕКТУРА РЕПОЗИТОРИЯ

```
my-code-assistant/ (root project)
├── src/
│   ├── index.ts                    ← Main entry point
│   ├── codeAssistant.ts            ← CodeAssistant class
│   ├── projectIndexer.ts           ← ProjectIndexer class
│   ├── gitHelper.ts                ← GitHelper class (MCP tools)
│   ├── chatbot.ts                  ← ChatBotCLI class
│   └── rag/
│       ├── ragPipeline.ts          ← RAG с цитатами
│       ├── conversationManager.ts  ← Память разговора
│       └── validators.ts           ← Валидация галлюцинаций
│
├── templates/                      ← Шаблоны конфигов
│   ├── config-web.json
│   ├── config-backend.json
│   ├── config-monorepo.json
│   └── config-default.json
│
├── docs/
│   ├── SETUP.md                    ← Как начать
│   ├── CONFIG.md                   ← Опции конфигурации
│   ├── ARCHITECTURE.md             ← Архитектура инструмента
│   ├── EXAMPLES.md                 ← Примеры использования
│   └── API.md                      ← TypeScript API
│
├── bin/
│   └── cli.js                      ← Executable для npm install -g
│
├── package.json
├── tsconfig.json
└── README.md
```

---

## 📥 КАК ИСПОЛЬЗОВАТЬ В СВОЁМ ПРОЕКТЕ

### Шаг 1: Установка
```bash
# Клонировать инструмент в свой проект
git clone https://github.com/user/my-code-assistant.git ./tools/code-assistant

# Или установить глобально
npm install -g my-code-assistant
```

### Шаг 2: Инициализация
```bash
cd ~/my-project
mca init  # My Code Assistant init

# Интерактивно создаст projectConfig.json
# Спросит о структуре проекта
# Автоматически определит папки
```

### Шаг 3: Индексирование
```bash
mca index
# Прочитает projectConfig.json
# Индексирует исходный код и документацию
# Сохранит индекс в node_modules/.code-assistant
```

### Шаг 4: Запуск чатбота
```bash
mca chat

🤖 My Code Assistant for: my-project
(main branch | 156 commits | 45K LOC)

You: Как работает authentication в этом проекте?
Assistant: [объясняет с цитатами из исходного кода]
```

### Шаг 5 (Optional): MCP интеграция
```bash
mca mcp --start
# Запускает MCP сервер на :3000
# Можно подключить другие приложения
```

---

## 🎬 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Понимание архитектуры
```
You: Объясни архитектуру этого проекта
Assistant:
Проект состоит из 3 основных слоёв:

1. Controllers (src/api/) - обрабатывают HTTP запросы
2. Services (src/services/) - бизнес-логика
3. Models (src/models/) - базы данных

[Показывает конкретные файлы с цитатами]
```

### Пример 2: Реализация функции
```
You: Как мне добавить аутентификацию через JWT?
Assistant: В этом проекте уже есть JWT логика в src/auth/jwtService.ts

[Показывает существующий код]
[Объясняет как его переиспользовать]
[Предлагает где добавить новый код]
```

### Пример 3: Дебаг проблемы
```
You: Почему auth middleware не работает?
Assistant: Посмотрев на код в src/middleware/auth.ts и src/app.ts...
[Объясняет где проблема]
[Показывает как исправить]
```

### Пример 4: Git контекст
```
You: /git-status
Assistant:
📍 Branch: feature/auth-update
Last 3 commits:
  • Add JWT validation (2h ago)
  • Fix token expiration (5h ago)
  • Initial auth setup (1d ago)
```

---

## ✅ ТРЕБОВАНИЯ

### MVP (День 20):
- ✅ ProjectIndexer работает с projectConfig.json
- ✅ GitHelper интегрирован через MCP
- ✅ CodeAssistant объединяет всё вместе
- ✅ ChatBot CLI с основными командами
- ✅ RAG с цитатами работает
- ✅ Документация SETUP.md и CONFIG.md

### Phase 2 (Future):
- Watch mode для автоперезагрузки индекса
- Auto-detection проектной структуры
- Веб UI для управления
- CLI для управления проектами
- Экспорт документации

---

## 🚀 РЕКОМЕНДУЕМЫЙ ПЛАН РЕАЛИЗАЦИИ

### Фаза 1 (4-5 часов):
1. **Параметризация RAG** (1 час)
   - ProjectIndexer - читает config, индексирует папки
   - Поддержка разных типов файлов

2. **Git MCP integration** (1 час)
   - GitHelper с основными функциями
   - Официальный MCP SDK

3. **CodeAssistant** (1 час)
   - Объединяет IndexerManager + Git + RAG

4. **ChatBot CLI** (1 час)
   - Параметризуемый интерфейс
   - /help, /git, /history и т.д.

5. **Документация** (1 час)
   - SETUP.md - инструкция установки
   - CONFIG.md - все опции
   - EXAMPLES.md - примеры

### Фаза 2 (Future):
- Watch mode
- Auto-detection
- Веб интерфейс
- Поддержка разных LLM

---

## 🎯 КЛЮЧЕВЫЕ ПРИНЦИПЫ

1. **Параметризуемость** - всё настраивается через projectConfig.json
2. **Независимость** - работает с ЛЮБЫМ проектом
3. **Стандартизация** - использует официальный MCP SDK
4. **Простота** - минимум зависимостей, максимум функциональности
5. **Расширяемость** - легко добавить новые инструменты

---

## 📊 УСПЕШНОЕ ЗАВЕРШЕНИЕ

Проект считается завершённым когда:

```bash
# Можно использовать в ЛЮБОМ проекте
$ mca init
$ mca index
$ mca chat

🤖 My Code Assistant for: [project-name]
You: Как работает [feature]?
Assistant: [Объясняет с цитатами из кода]
```

---

## 💡 ВАЖНЫЕ МОМЕНТЫ

### Про RAG цитирование:
- Все ответы ДОЛЖНЫ иметь цитаты [1], [2] и т.д.
- Цитаты указывают на конкретные файлы и строки кода
- Если нет информации в коде - честный отказ

### Про Git:
- Git информация НЕ индексируется в RAG
- Git используется только через MCP инструменты
- Позволяет запросить "что я делал последний месяц?"

### Про конфигурацию:
- projectConfig.json это ЕДИНСТВЕННЫЙ файл конфигурации
- Все остальное должно быть auto-detected или иметь reasonable defaults
- Должны быть шаблоны для разных типов проектов

### Про расширяемость:
- Новые инструменты добавляются как MCP tools
- Система должна позволять добавить свои анализаторы кода
- Должна поддерживать плагины (в будущем)

---

## 🔗 СВЯЗЬ С ТЕКУЩИМ ПРОЕКТОМ

Текущий RAG-Ollama-Demo это **прототип** для My Code Assistant:
- RAG Pipeline готов - скопировать как есть
- ConversationManager готов - переиспользовать
- ChatBot CLI готов - адаптировать под параметризацию

**НО**: Новый проект будет **generic**, не привязан к одному RAG-Ollama проекту.

---

## 📝 ГОТОВНО К ИСПОЛЬЗОВАНИЮ

Этот промпт содержит ВСЮ информацию которая нужна AI агенту для:
- Понимания видения проекта
- Правильной архитектуры
- Правильной реализации компонентов
- Создания документации
- Планирования фаз

**Просто скопируй этот текст и дай AI агенту**, и тот поймёт что нужно делать!
